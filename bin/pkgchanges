#!/bin/sh
if [ $# -ne 2 ]; then
	echo "usage: pkgchanges [core/extra/community/multilib] [package]"
	exit 1
fi

if [ $1 == "core" ] || [ $1 == "extra" ]; then
	REPO="packages"
elif [ $1 == "community" ] || [ $1 == "multilib" ]; then
	REPO="community"
else
	echo "error: $1 is not a valid repository"
	exit 1
fi

xdg-open "https://projects.archlinux.org/svntogit/$REPO.git/log/trunk?h=packages/$2"

